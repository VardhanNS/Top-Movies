import { BoldTextDirective } from './bold-text.directive';

describe('BoldTextDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldTextDirective();
    expect(directive).toBeTruthy();
  });
});
